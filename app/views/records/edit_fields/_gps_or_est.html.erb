<%#Generated by the Tenant Authority Generator %>
<% if request.base_url.include? 'iaff' %>
	<% gps_or_est = IaffGpsOrEstService.new %>
	<%= f.input :gps_or_est,
		collection: gps_or_est.select_active_options,
		include_blank: true,
		item_helper: gps_or_est.method(:include_current_value),
		input_html: { class: 'form-control' } %>
<% else %>
<%= f.input key,
            as: :multi_value,
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/local/gps_or_est",
                      'autocomplete' => key }
            },
            required: f.object.required?(key) %>
<% end %>