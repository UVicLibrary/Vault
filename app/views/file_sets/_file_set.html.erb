<%# This view is rendered when displaying search results for file sets.
    The only way to see this from the interface is to click the keyword facet link
    on a file set page. %>
<%= render "catalog/document", document: file_set, document_counter: file_set_counter %>