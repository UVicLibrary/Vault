<%# unless the parent work is downloadable, hide the download icon %>
<% if request.base_url.include? "vault" %>
      <style>
          #download { display: none !important; }
      </style>
<% end %>